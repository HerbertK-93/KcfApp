 import 'package:flutter/material.dart';

class LandAcquisitionScreen extends StatelessWidget {
  const LandAcquisitionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Deposit'),
      ),
      body: Center(
        child: const Text('Deposit Screen Content'),
      ),
    );
  }
}